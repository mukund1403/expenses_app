import { Typography } from '@mui/material';

export default function AnalyticsPage() {
  return (
    <Typography
      variant='h4'
      sx={{
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      Coming Soon!
    </Typography>
  );
}
